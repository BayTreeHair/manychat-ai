{
  "name": "baytree-ai",
  "version": "1.0.0",
  "description": "Bun server that classifies ManyChat messages and triggers the matching flow.",
  "type": "module",
  "module": "index.js",
  "scripts": {
    "start": "bun run index.js",
    "dev": "bun --watch index.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@openrouter/sdk": "^0.12.35",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  }
}
